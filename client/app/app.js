angular.module('rain', [
	'rain.services',
	'ngRoute'
])

//TODO: client side routing
.config(function($routeProvider){
	.when('/', {
		templateUrl: 
		controller:
	})
	.otherwise({
		redirectTo:
	})
})